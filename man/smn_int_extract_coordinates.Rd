% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/int_extract_coordinates.R
\name{smn_int_extract_coordinates}
\alias{smn_int_extract_coordinates}
\title{Extract Station Coordinates Robustly (internal)}
\usage{
smn_int_extract_coordinates(
  station,
  max_attempts = 10,
  encoding = "UTF-8",
  add_meta_cols = FALSE
)
}
\arguments{
\item{station}{Station code (character or numeric).}

\item{max_attempts}{Integer. Maximum retries for the metadata download (default \code{10}).}

\item{encoding}{Character. Encoding for \code{readLines()} (default \code{"UTF-8"}).}

\item{add_meta_cols}{Logical. If \code{TRUE}, include \code{station} and \code{source_url}
as explicit columns in the returned data (default \code{FALSE}). These are
always attached as attributes for provenance.}
}
\value{
A one-row \code{data.frame} with columns \code{latitude}, \code{longitude}, \code{altitude}.
Attributes \code{station} and \code{source_url} are attached.
}
\description{
Retrieves the metadata lines for a given station using the URL generated by
\code{smn_int_get_url()}, searches for lines containing latitude/longitude/altitude
labels (case-insensitive, ES/EN variants), and extracts the first numeric
value from each line. If a coordinate cannot be extracted or fails range
validation, \code{NA} is returned for that coordinate.
}
\examples{
NULL
}
\keyword{internal}
