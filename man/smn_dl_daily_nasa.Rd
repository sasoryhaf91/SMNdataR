% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dl_daily_nasa.R
\name{smn_dl_daily_nasa}
\alias{smn_dl_daily_nasa}
\title{Download Daily NASA POWER (MERRA-2) Data for a Named Point}
\usage{
smn_dl_daily_nasa(
  station,
  lat,
  lon,
  altitude = NA_real_,
  start_date = "1984-01-01",
  end_date = Sys.Date(),
  vars = c("T2M_MAX", "T2M_MIN", "RH2M", "PRECTOTCORR", "EVLAND"),
  community = "AG",
  retries = 5,
  sleep_sec = 3,
  output_format = c("all", "reduce")
)
}
\arguments{
\item{station}{Character. A label or node name to tag the time series (e.g., "Toluca_Node").}

\item{lat}{Numeric. Latitude in decimal degrees (WGS84).}

\item{lon}{Numeric. Longitude in decimal degrees (WGS84).}

\item{altitude}{Numeric (optional). Altitude of the point (m a.s.l.). Default \code{NA_real_}.}

\item{start_date}{Start date (YYYY-MM-DD). If earlier than 1984-01-01, it is
automatically adjusted to \code{1984-01-01} (POWER coverage).}

\item{end_date}{End date (YYYY-MM-DD). Default \code{Sys.Date()}.}

\item{vars}{Character vector of POWER parameter short names. Defaults to a compact
set: \code{c("T2M_MAX","T2M_MIN","RH2M","PRECTOTCORR","EVLAND")}.}

\item{community}{POWER community. Default \code{"AG"}. (Common alternatives: \code{"SSE"}, \code{"RE"})}

\item{retries}{Integer. Max GET retries on failure (default 5).}

\item{sleep_sec}{Numeric. Seconds to sleep between retries (default 3).}

\item{output_format}{Either \code{"all"} (default) or \code{"reduce"}. The \code{"reduce"} option
keeps only \verb{station, latitude, longitude, altitude, date} and the requested variables.}
}
\value{
A \code{data.frame} ordered by \code{date} with columns:
\verb{station, latitude, longitude, altitude, date, <requested POWER variables>}.
POWER missing values \code{-999} are returned as \code{NA}.
}
\description{
Downloads daily NASA POWER variables for a specific point (lat/lon) and tags
the result with a user-defined \code{station} (node) name so you can join/trace later.
}
\examples{
\dontrun{
nasa_df <- smn_dl_daily_nasa(
  station = "Toluca_Node",
  lat = 19.289, lon = -99.657, altitude = 2670,
  start_date = "2020-01-01", end_date = "2020-12-31",
  vars = c("T2M_MAX","T2M_MIN","PRECTOTCORR"),
  output_format = "reduce"
)
}
}
